{
  "name": "enid-mandate-relay",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "description": "Event-time MongoDB proof of agent continuity with bounded authority.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/relay-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "release:check": "node --test tests/release.test.mjs && node scripts/release-check.mjs"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  }
}
